import { Inter, Allura } from 'next/font/google';

export const inter = Inter({
    subsets: ['latin'],
    weight: '400',
});

export const allura = Allura({
    subsets: ['latin'],
    weight: '400', 
});
